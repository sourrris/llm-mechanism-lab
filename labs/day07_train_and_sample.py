"""Day 07 scaffold: train mini-GPT and separate logits from decoding.

Use ``data/tiny_corpus.txt`` or a corpus you can legally redistribute. Keep the
model small enough that the experiment completes locally. Save checkpoints
outside Git or under an ignored extension.
"""


def main() -> None:
    # TODO 1: train/load the Day 02 tokenizer and encode the corpus.
    # TODO 2: sample random contiguous training blocks.
    # TODO 3: train MiniGPT while recording train and held-out loss.
    # TODO 4: save one checkpoint and the complete configuration.
    # TODO 5: generate the same prompt under the required decoding grid.
    raise NotImplementedError("Complete during Day 07")


if __name__ == "__main__":
    main()
